# dsh-doudizhu bundle patch
#
# 安装入口：
#   dsh plugin --profile web add <本地路径 | AwesomeHou/dsh-doudizhu>
#
# 本文件是 `dsh.bundle.patch` 层：profile 启动时会把本插件插入 bundle 栈。
# M1 起正式启用。
- insert:
    - id: doudizhu
      name: 'dsh-doudizhu'
