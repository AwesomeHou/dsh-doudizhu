{
  "name": "dsh-doudizhu-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply dsh-doudizhu --local",
    "migrate:prod": "wrangler d1 migrations apply dsh-doudizhu --remote"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
